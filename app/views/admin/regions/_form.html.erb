<%= form_with(model: admin_region, local: true) do |form| %>
  <% if admin_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_region.errors.count, "error") %> prohibited this admin_region from being saved:</h2>

      <ul>
      <% admin_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :region_name %>
    <%= form.text_field :region_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
